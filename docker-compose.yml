version: '3'
services:
  app:
    build: .
    container_name: node_app
    ports:
      - "3000:3000"
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: test_container
    depends_on:
      - app
